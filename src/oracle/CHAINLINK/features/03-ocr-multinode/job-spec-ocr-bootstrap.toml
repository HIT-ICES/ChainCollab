type               = "offchainreporting"
schemaVersion      = 1
name               = "OCR Bootstrap Job"
externalJobID      = "aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee"
isBootstrapPeer    = true
evmChainID         = 3456
keyBundleID        = "<YOUR_OCR_KEY_BUNDLE_ID>"

# OCR v1 使用 OffchainAggregator 合约地址
contractAddress    = "<YOUR_OCR_V1_AGGREGATOR_ADDRESS>"

# OCR v1 p2p bootstrap peers (bootstrap 节点自身不需要 bootstrap 地址)
p2pBootstrapPeers  = []